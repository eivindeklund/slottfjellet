# The scp/sftp URL we use
SLOTTSFJELLET_URL=sftp://USER@HOST/DIRECTORY

# The subdirectory on the server we upload to
SLOTTSFJELLET_WWW_SUBDIR=www

# The password we use; delete or leave empty if you would rather be prompted for password
LFTP_PASSWORD=